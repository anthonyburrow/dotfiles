############
# Internet #
############

# I don't know about this one...
systemctl enable NetworkManager.service

# I think you have to reboot before this next step for some reason
nmcli d
nmcli r wifi on
nmcli d wifi list
nmcli d wifi connect "OwO x3" password "JamesFranco666"

# also you can do this instead (the other didn't work this past time)
nmtui


########
# sudo #
########

pacman -Syu sudo
useradd -m masamune
passwd masamune

usermod --append --groups wheel masamune

# then edit sudoers file, uncomment the wheel line
visudo
"## Uncomment to allow members of group wheel to execute any command
 %wheel ALL=(ALL) ALL"

hostnamectl set-hostname <machine_name>

#########
# Audio #
#########

sudo pacman -Syu pulseaudio pulsemixer

# may need a reboot


########
# rice #
########

sudo pacman -Syu git i3-wm xorg-server xorg-xinit rxvt-unicode feh rofi
# Choose ttf-dejavu for fonts

# Idk why but this was also needed for rofi to work
sudo locale-gen en_US.UTF-8

# install yay (AUR)
sudo pacman -Syu --needed base-devel
git clone https://aur.archlinux.org/yay.git .yay
cd .yay
makepkg -si

yay -S polybar

# polybar fonts
sudo pacman -S nerd-fonts

git config --global user.email "anthony.r.burrow@gmail.com"
git config --global user.name "Anthony Burrow"

git clone --bare https://github.com/anthonyburrow/dotfiles.git $HOME/.dotfiles
alias dotfiles='/usr/bin/git --git-dir=$HOME/.dotfiles/ --work-tree=$HOME'
# May need to remove (old) .bash_profile, .bashrc
dotfiles checkout
dotfiles status

# if dotfiles lists untracked files:
dotfiles config --local status.showUntrackedFiles no

# can now do:
# dotfiles add <some files>
# dotfiles commit -m "message"
# dotfiles push origin master


#########################
# GitHub Authentication #
#########################

ssh-keygen -t ed25519 -C "anthony.r.burrow@gmail.com"
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519

cat ~/.ssh/id_ed25519.pub
# Copy-paste output to https://github.com/settings/keys -> New SSH key

git config --global url."git@github.com:".insteadOf "https://github.com/"

# For things that are already cloned (e.g. dotfiles):
# git remote set-url origin git@github.com:username/repo.git

dotfiles remote set-url origin git@github.com:anthonyburrow/dotfiles.git


##################
# Other Programs #
##################

sudo pacman -Syu firefox
yay -S visual-studio-code-bin

nerd fonts SauceCodePro -> ~/.fonts
unzip, tar
python, python-pip
discord
flux
ssh keys
