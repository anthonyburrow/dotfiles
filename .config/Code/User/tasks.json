{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Auto Build",
            "type": "shell",
            "command": "~/.bin/build",
            "args": ["${file}"],
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}